[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-memory-strands-agents"
version = "0.2.0"
description = "AgentMemory agent memory as tools for the Strands Agents SDK."
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "SurrealDB" }]
keywords = ["agent_memory", "surrealdb", "strands", "agents", "memory", "llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "strands-agents>=1.0",
    "surrealdb[memory]>=3.0.0b8",
]

[project.optional-dependencies]
bedrock = ["strands-agents[bedrock]>=1.0"]
dev = ["pytest>=8.0"]

[project.urls]
Homepage = "https://surrealdb.com/agent-memory"
Documentation = "https://surrealdb.com/docs/agent-memory"
Strands = "https://strandsagents.com"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_memory_strands"]
